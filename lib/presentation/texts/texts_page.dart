import 'package:flutter/material.dart';

class TextsPage extends StatelessWidget {
  const TextsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Texts page'),
      ),
    );
  }
}
